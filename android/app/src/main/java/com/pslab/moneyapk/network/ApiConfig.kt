package com.pslab.moneyapk.network

/**
 * Адрес бэкенда.
 *
 * Сейчас (шаг 2 фазы 4) бэкенд ещё не задеплоен на VPS, поэтому приложение
 * ходит на локальный компьютер разработчика, где запущен `uvicorn` (см.
 * backend/README.md).
 *
 * Используется 127.0.0.1 (а не стандартный для эмулятора адрес 10.0.2.2) вместе
 * с `adb reverse tcp:8000 tcp:8000` (см. android/README.md, "Как протестировать
 * логин") — это обходит VPN/файрвол на компьютере разработчика, так как трафик
 * идёт через ADB-канал, а не через виртуальную сеть эмулятора.
 * На реальном устройстве (например Galaxy S25) этот адрес работать не будет —
 * там нужен либо адрес компьютера в локальной сети, либо (правильный путь для
 * реального использования) уже задеплоенный бэкенд на VPS с HTTPS.
 *
 * TODO(фаза 4, после деплоя бэкенда): заменить на "https://<домен>/".
 */
object ApiConfig {
    const val BASE_URL = "http://127.0.0.1:8000/"
}
