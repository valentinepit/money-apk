package com.pslab.moneyapk.network

/**
 * Адрес бэкенда.
 *
 * Фаза 6: бэкенд задеплоен на VPS пользователя и доступен снаружи по обычному
 * HTTP (без домена/HTTPS пока — см. claude/plan.md, фаза 6). Приложение теперь
 * ходит именно туда, а не на локальный компьютер разработчика через эмулятор.
 *
 * Порт 8001, а не стандартный 8000 — на сервере порт 8000 занят посторонним
 * процессом (см. claude/plan.md, фаза 6), бэкенд опубликован на 8001.
 *
 * Раньше здесь был "http://127.0.0.1:8000/" для работы через эмулятор +
 * `adb reverse` (см. историю в git) — если понадобится вернуться к локальной
 * разработке, старое значение можно найти в истории коммитов.
 *
 * TODO(фаза 6, когда появится домен): заменить на "https://<домен>/" и убрать
 * cleartext-исключение для этого IP из network_security_config.xml.
 */
object ApiConfig {
    const val BASE_URL = "http://185.122.184.92:8001/"
}
